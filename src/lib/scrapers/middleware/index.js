const scrapeIt = require("scrape-it");
const axios = require('axios')
const moment = require('moment');
const { Tjajal } = require ('../functions/Tjajal')

const scrapper = ()=>{}

module.exports = {
    scrapeRecord,
    scrapeUrl,
}